import { Link } from 'react-router-dom'
import { TrendingUp, PieChart, Wallet, AlertTriangle, LineChart } from 'lucide-react'
const STEPS=['Configure your preferences in Settings.','Enter your income.','Record your expenses.','Set planned amounts in Monthly Budget.','Review the Dashboard.','Monitor annual progress.']
const FEATURES=[{icon:TrendingUp,label:'Track Income & Expenses',tone:'info'},{icon:PieChart,label:'Compare Planned vs Actual',tone:'analytic'},{icon:Wallet,label:'Monitor Savings',tone:'positive'},{icon:AlertTriangle,label:'Identify Overspending',tone:'warning'},{icon:LineChart,label:'Review Financial Trends',tone:'negative'}] as const
const toneBg:Record<string,string>={info:'bg-info-bg text-info',analytic:'bg-analytic-bg text-analytic',positive:'bg-positive-bg text-positive',warning:'bg-warning-bg text-warning',negative:'bg-negative-bg text-negative'}
export default function StartHere(){return <div className="space-y-6"><div className="bg-white rounded-card shadow-card p-6"><h2 className="text-xl font-semibold text-brand-navy mb-2">Welcome to Your Personal Monthly Budget Planner</h2><p className="text-slate-600 mb-4">Plan your monthly income, track expenses, compare planned vs. actual spending, monitor savings, identify overspending, and review your monthly and annual performance — all in one place.</p><h3 className="text-sm font-semibold text-slate-700 mb-2">Quick Start</h3><ol className="space-y-1.5">{STEPS.map((step,i)=><li key={i} className="flex items-center gap-2 text-sm text-slate-600"><span className="w-5 h-5 rounded-full bg-brand-navy text-white text-xs flex items-center justify-center flex-shrink-0">{i+1}</span>{step}</li>)}</ol></div><div className="grid grid-cols-2 md:grid-cols-5 gap-3">{FEATURES.map(({icon:Icon,label,tone})=><div key={label} className="bg-white rounded-card shadow-card p-4 flex flex-col items-center text-center gap-2"><span className={`p-2 rounded-full ${toneBg[tone]}`}><Icon size={18}/></span><span className="text-xs font-medium text-slate-700">{label}</span></div>)}</div><div className="bg-brand-navy text-white rounded-card p-5 text-sm italic">"A budget gives you permission to spend without guilt and to save without sacrifice."</div><div className="flex flex-wrap gap-2">{['settings','income','expenses','budget','dashboard','annual-summary'].map(path=><Link key={path} to={`/${path}`} className="px-4 py-2 rounded-lg bg-white shadow-card text-sm font-medium text-brand-navy hover:bg-slate-50 capitalize">Go to {path.replace('-',' ')} →</Link>)}</div></div>}