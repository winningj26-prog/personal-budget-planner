import { useEffect,useState } from 'react'
import { supabase } from '../lib/supabaseClient'
import { useSettings } from '../lib/SettingsContext'
import { formatCurrency,formatDate } from '../lib/format'
import KpiCard from '../components/KpiCard'
import DataTable,{Column} from '../components/DataTable'
import { Wallet,Hash,TrendingUp,Plus } from 'lucide-react'
interface IncomeRow{id:string;date:string;category_id:string;category_name?:string;description:string;amount:number}
interface Category{id:string;name:string}
export default function Income(){const{settings}=useSettings();const[rows,setRows]=useState<IncomeRow[]>([]);const[categories,setCategories]=useState<Category[]>([]);const[form,setForm]=useState({date:'',category_id:'',description:'',amount:''});async function load(){const{data:cats}=await supabase.from('income_categories').select('id,name');setCategories(cats??[]);const s=`${settings.year}-${String(settings.month).padStart(2,'0')}-01`,e=`${settings.year}-${String(settings.month).padStart(2,'0')}-31`;const{data}=await supabase.from('income').select('id,date,category_id,description,amount,income_categories(name)').gte('date',s).lte('date',e).order('date',{ascending:false});setRows((data??[]).map((r:any)=>({...r,category_name:r.income_categories?.name??'—'})))}useEffect(()=>{load()},[settings.month,settings.year]);async function addIncome(){if(!form.date||!form.amount)return;const{data:userData}=await supabase.auth.getUser();await supabase.from('income').insert({date:form.date,category_id:form.category_id||null,description:form.description,amount:Number(form.amount),user_id:userData.user?.id});setForm({date:'',category_id:'',description:'',amount:''});load()}const total=rows.reduce((s,r)=>s+Number(r.amount),0),count=rows.length,average=count?total/count:0;const columns:Column<IncomeRow>[]=[{header:'Date',accessor:r=>formatDate(r.date)},{header:'Category',accessor:r=>r.category_name},{header:'Description',accessor:r=>r.description},{header:'Amount',accessor:r=>formatCurrency(r.amount,settings.currency),align:'right'}];return <div className="space-y-6"><div className="grid grid-cols-3 gap-3"><KpiCard label="Total Income" value={formatCurrency(total,settings.currency)} icon={Wallet} tone="info"/><KpiCard label="Transactions" value={String(count)} icon={Hash} tone="neutral"/><KpiCard label="Average Income" value={formatCurrency(average,settings.currency)} icon={TrendingUp} tone="positive"/></div><div className="bg-white rounded-card shadow-card p-4"><h3 className="text-sm font-semibold text-slate-700 mb-3">Add Income</h3><div className="grid grid-cols-5 gap-2 items-end"><label className="text-xs text-slate-500 flex flex-col gap-1">Date<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})} className="border border-slate-200 rounded-md px-2 py-1.5 text-sm"/></label><label className="text-xs text-slate-500 flex flex-col gap-1">Category<select value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})} className="border border-slate-200 rounded-md px-2 py-1.5 text-sm"><option value="">Select…</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="text-xs text-slate-500 flex flex-col gap-1">Description<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="border border-slate-200 rounded-md px-2 py-1.5 text-sm"/></label><label className="text-xs text-slate-500 flex flex-col gap-1">Amount<input type="number" step="0.01" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} className="border border-slate-200 rounded-md px-2 py-1.5 text-sm"/></label><button onClick={addIncome} className="px-3 py-2 rounded-md bg-brand-navy text-white text-sm flex items-center gap-1 justify-center"><Plus size={14}/> Add</button></div></div><DataTable columns={columns} rows={rows} totalRow={['','','Total Income',formatCurrency(total,settings.currency)]} emptyLabel="No income recorded for this month yet."/></div>}